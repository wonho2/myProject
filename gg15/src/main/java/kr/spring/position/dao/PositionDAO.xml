<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.position.dao.PositionDAO">

<!-- 게시물 목록 -->
	<select id="selectBoardListAll" parameterType="map" resultType="positionVO">
		SELECT
		   *
		FROM (SELECT 
		        a.*,
		        rownum rnum
		      FROM(SELECT
		              p.pos_num,
		              p.pos_type,
		              <![CDATA[
		              REPLACE(REPLACE(p.pos_title,'<','&lt;'),'>','&gt;') pos_title,
		              ]]>
		              m.mem_nick,
		              p.pos_date,
		              p.pos_view,
		              p.pos_fav 
		              FROM position p JOIN member_detail m ON p.mem_num = m.mem_num ORDER BY p.pos_num DESC)a)
    	<![CDATA[
    	WHERE rnum >= #{start} AND rnum <= #{end}
    	]]>
	</select>
	
<!-- 게시물 상세 페이지 -->
	<select id="selectBoard" parameterType="Integer" resultType="positionVO">
		SELECT 
			p.*,
			m.mem_nick  
			FROM position p JOIN member_detail m ON p.mem_num = m.mem_num 
			WHERE pos_num = #{pos_num}
	</select>
	
<!-- 게시물 수정 -->
	<update id="updateBoard" parameterType="positionVO">
		UPDATE position SET	
			<if test="pos_filename != ''">
				pos_uploadfile = #{pos_uploadfile},
				pos_filename = #{pos_filename},
			</if>
			pos_title = #{pos_title},
			pos_content = #{pos_content} 
			WHERE pos_num = #{pos_num}
	</update>
	
<!-- 질문 : 인기순으로 정렬할 때 데이터베이스에 추천수 데이터가 있어야 하는지, 아니면 vo 만으로도 정렬이 가능한지 -->
<!-- 게시물 댓글 리스트 (최신순) -->
	<select id="selectCommentList_recent" parameterType="Integer" resultType="positionCommentVO">
 		SELECT 
			p.*,
			m.mem_nick 
			FROM position_comment p JOIN member_detail m ON p.mem_num = m.mem_num 
			WHERE pos_num = #{pos_num} ORDER BY p.poc_date DESC
 	</select>
	
</mapper>
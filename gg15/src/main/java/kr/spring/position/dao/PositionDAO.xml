<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.position.dao.PositionDAO">
<!-- 게시물 목록 -->
	<select id="selectBoardList" parameterType="map" resultType="positionVO">
		SELECT
		   *
		FROM (SELECT 
		        a.*,
		        rownum rnum
		      FROM(SELECT
		              p.pos_num,
		              p.pos_type,
		              <![CDATA[
		              REPLACE(REPLACE(p.pos_title,'<','&lt;'),'>','&gt;') pos_title,
		              ]]>
		              m.mem_nick,
		              p.pos_date,
		              p.pos_view,
		              p.pos_fav 
		              FROM position p JOIN member_detail m ON p.mem_num = m.mem_num ORDER BY p.pos_num DESC)a)
    	<![CDATA[
    	WHERE rnum >= #{start} AND rnum <= #{end}
    	]]>
	</select>
	
<!-- 게시물 상세 페이지 -->
	<select id="selectBoard" parameterType="Integer" resultType="positionVO">
		SELECT 
			p.*,
			m.mem_nick  
			FROM position p JOIN member_detail m ON p.mem_num = m.mem_num 
			WHERE pos_num = #{boardNum}
	</select>
	
<!-- 게시물 수정 -->
	<update id="updateBoard" parameterType="positionVO">
		UPDATE position SET	
			<if test="pos_filename != ''">
				pos_uploadfile = #{pos_uploadfile},
				pos_filename = #{pos_filename},
			</if>
			pos_title = #{pos_title},
			pos_content = #{pos_content} 
			WHERE pos_num = #{pos_num}
	</update>
	
<!--====== 댓글 ========  -->	
	<select id="selectListReply" parameterType="map"
 	                     resultType="positionCommentVO">
 		SELECT
 		   *
 		FROM (SELECT 
 		        m.*, 
 		        rownum rnum 
 		      FROM (SELECT 
                      *
                    FROM (SELECT 
                            poc_num,
 		                    poc_content,
 		                    TO_CHAR(poc_date,'YYYY-MM-DD HH24:MI:SS') poc_date,
 		                    pos_num,
 		                    mem_num
                          FROM position_comment where pos_num=#{pos_num}) r 
                               JOIN member_detail m
                               USING(mem_num)
                               ORDER BY r.poc_num DESC)m)
                              
 		<![CDATA[
 		WHERE rnum >= #{start} AND rnum <= #{end}
 		]]>                                 
 	</select>
	
</mapper>